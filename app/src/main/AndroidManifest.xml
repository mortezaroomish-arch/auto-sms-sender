<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ارسال پیامک -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- دریافت پیامک (برای لغوِ اشتراکِ خودکار) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- تشخیصِ وضعیتِ تماس (برای پیامکِ تماسِ بی‌پاسخ) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- خواندنِ شماره‌ی تماس‌گیرنده‌ی بی‌پاسخ -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- خواندن مخاطبین گوشی -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- نمایش اعلان (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- سرویس پیش‌زمینه برای ارسال طولانی -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- بیدار نگه داشتن پردازنده هنگام ارسال -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- زمان‌بندی مجدد پس از روشن شدن گوشی -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".AutoSmsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoSmsSender">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AutoSmsSender">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- زمان‌بندی مجدد پس از ری‌استارت گوشی -->
        <receiver
            android:name=".work.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- دریافتِ پیامکِ ورودی برای لغوِ اشتراکِ خودکار -->
        <receiver
            android:name=".work.SmsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- تشخیصِ تماسِ بی‌پاسخ برای ارسالِ پیامکِ خودکار -->
        <receiver
            android:name=".work.PhoneStateReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!--
            سرویسِ پیش‌زمینه‌ی WorkManager باید نوعِ dataSync را صریحاً اعلام کند،
            وگرنه در اندروید ۱۴ به بالا هنگام startForeground برنامه کرش می‌کند.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
